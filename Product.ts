class product {

public name: string
public id: number
public price: number

constructor(name: string, id: number, price: number) {
    this.name = name;
    this.id = id;
    this.price = price;
    }
}